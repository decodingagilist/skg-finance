# SKG Finance — Demo Landing Page

Single-file landing page for SKG Finance (lending broker demo site).

- `index.html` — the full standalone page (services, 9 financial calculators, booking widget, chatbot, contact forms). Self-contained except for the Google Fonts and Chart.js CDN references in `<head>`.
- `broker-photo.jpg` — source headshot embedded (base64) in the hero section of `index.html`.

## Notes

- Analytics (GA4/Meta Pixel), n8n webhooks, WhatsApp number, and Calendly URL are placeholders in the `SKG` config object near the top of `index.html` — fill these in before going live.
- Phone: +61 404 213 313. Email: simar@skgfinance.com.au.
- Stamp Duty and Borrowing Power calculators are indicative estimates only, not financial advice.
